# Build output
zig-out/
.zig-cache/

# Models — large binary files, track separately or via Git LFS
models/*.onnx
models/*.onnx.data
models/vocab.bin
models/merges.bin

# Local config (may contain secrets)
icarium.toml

# PostgreSQL temp files
*.pid

# Planning docs — local only, not for version control
ARCHITECTURE.md
CRITIQUE.md
HERMES_COMPARISON.md
GEAR_01_MODEL.md
PLAN.md
PROMPT.md
TUI_PLAN.md

# OS
.DS_Store
